[Pod]
PodName=nc
# Published on all interfaces because Bunkerweb runs as a different rootless
# user and reaches this through the host address; a loopback bind is not
# reachable that way and yields 502. firewalld admits only ssh, http and https,
# so this port is not exposed to the LAN despite the bind address.
PublishPort=8080:80
