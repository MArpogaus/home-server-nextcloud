[Unit]
Description=Pre-snapshot pg_dumpall for {{ service_user }}

[Service]
Type=oneshot
ExecStart=systemd-run --machine={{ service_user }}@ --user --wait --pipe --collect {{ service_home }}/bin/pre-snapshot-pg-dump.sh
